# Gradle
.gradle/
build/
gradle-wrapper.jar        # already tracked - consider ignoring

# Local config (SENSITIVE - should definitely be ignored)
local.properties          # contains SDK path, sometimes API keys

# IDE
.idea/
*.iml
*.iws
*.ipr

# OS
.DS_Store
Thumbs.db

# Keystore / signing (SENSITIVE)
*.jks
*.keystore
keystore.properties